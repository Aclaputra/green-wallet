import { RpCurrencyDirective } from './rp-currency.directive';

describe('RpCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new RpCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
