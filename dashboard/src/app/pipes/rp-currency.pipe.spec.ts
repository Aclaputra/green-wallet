import { RpCurrencyPipe } from './rp-currency.pipe';

describe('RpCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new RpCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
